import { GenericPage, TitleBar } from 'cms-admin'
import * as React from 'react'

export const DashboardPage = (
	<GenericPage pageName="dashboard">
		<TitleBar>Hranipex admin</TitleBar>
	</GenericPage>
)
